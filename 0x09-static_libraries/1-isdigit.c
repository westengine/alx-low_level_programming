#include "main.h"
		
/**
 * _isdigit - checks for a digit (0 through 9)
 * @n: int to be checked
 * Return: 1 if n is a digit, 0 otherwise
 */

int _isdigit(int n)
{
	return (n >= '0' && n <= '9');
}
